<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
  <div class="container">
    <a class="navbar-brand" href="{{ path('app_manga_index') }}">Manga Manager</a>
    <div class="ms-auto">
      {% if app.user %}
      <a class="btn btn-outline-secondary btn-sm">
          </i>{{ app.user.email }}
        </a>
        <a class="btn btn-outline-secondary btn-sm" href="{{ path('app_logout') }}">
          <i class="bi bi-box-arrow-right me-1"></i>Logout
        </a>
      {% else %}
        <a class="btn btn-outline-primary btn-sm me-2" href="{{ path('app_register') }}">Register</a>
        <a class="btn btn-primary btn-sm" href="{{ path('app_login') }}">Login</a>
      {% endif %}
    </div>
  </div>
</nav>